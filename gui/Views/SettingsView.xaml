<UserControl x:Class="MedicAIGUI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="Settings" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- General Settings -->
            <GroupBox Header="General" Margin="0,0,0,15">
                <StackPanel Margin="10">
                    <CheckBox x:Name="AutoStartBot" Content="Auto-start bot on application launch" 
                              IsChecked="{Binding AutoStartBot, Mode=TwoWay}"/>
                    <CheckBox x:Name="MinimizeToTray" Content="Minimize to system tray" 
                              IsChecked="{Binding MinimizeToTray, Mode=TwoWay}"/>
                </StackPanel>
            </GroupBox>

            <!-- Vaccinator Overlay Settings -->
            <GroupBox Header="Vaccinator Overlay" Margin="0,0,0,15">
                <StackPanel Margin="10">
                    <CheckBox x:Name="EnableVaccinatorOverlay" Content="Show Vaccinator resistance overlay" 
                              IsChecked="{Binding ShowVaccinatorOverlay, Mode=TwoWay}"/>
                    <Slider x:Name="OverlayOpacity" Minimum="0.3" Maximum="1.0" 
                            Value="{Binding OverlayOpacity, Mode=TwoWay}" Width="200" Margin="0,5,0,0"/>
                    <TextBlock Text="Opacity:" Margin="0,2,0,0"/>
                </StackPanel>
            </GroupBox>

            <!-- Keybinds Section -->
            <GroupBox Header="Keybinds" Margin="0,0,0,15">
                <StackPanel Margin="10">
                    <!-- Vaccinator Cycle Key -->
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <Label Content="Cycle Resistance Key:" Width="150"/>
                        <TextBox x:Name="VaccinatorCycleKey" Text="{Binding VaccinatorCycleKey, Mode=TwoWay}" Width="60"/>
                        <ComboBox x:Name="VaccinatorCycleModifier" SelectedItem="{Binding VaccinatorCycleModifier, Mode=TwoWay}" Width="80" Margin="5,0">
                            <ComboBoxItem>None</ComboBoxItem>
                            <ComboBoxItem>Control</ComboBoxItem>
                            <ComboBoxItem>Alt</ComboBoxItem>
                            <ComboBoxItem>Shift</ComboBoxItem>
                            <ComboBoxItem>Windows</ComboBoxItem>
                        </ComboBox>
                    </StackPanel>

                    <!-- Uber Pop Keybind -->
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <Label Content="Uber Pop Keybind:" Width="150"/>
                        <TextBox x:Name="UberKeybind" Text="{Binding UberKeybind, Mode=TwoWay}" Width="60"/>
                        <ComboBox x:Name="UberModifier" SelectedItem="{Binding UberModifier, Mode=TwoWay}" Width="80" Margin="5,0">
                            <ComboBoxItem>None</ComboBoxItem>
                            <ComboBoxItem>Control</ComboBoxItem>
                            <ComboBoxItem>Alt</ComboBoxItem>
                            <ComboBoxItem>Shift</ComboBoxItem>
                            <ComboBoxItem>Windows</ComboBoxItem>
                        </ComboBox>
                    </StackPanel>

                    <!-- Optional: Custom key for switching resist to match bot -->
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <Label Content="Match Bot Resistance Key:" Width="150"/>
                        <TextBox x:Name="MatchResistKey" Text="{Binding MatchResistKey, Mode=TwoWay}" Width="60"/>
                        <ComboBox x:Name="MatchResistModifier" SelectedItem="{Binding MatchResistModifier, Mode=TwoWay}" Width="80" Margin="5,0">
                            <ComboBoxItem>None</ComboBoxItem>
                            <ComboBoxItem>Control</ComboBoxItem>
                            <ComboBoxItem>Alt</ComboBoxItem>
                            <ComboBoxItem>Shift</ComboBoxItem>
                            <ComboBoxItem>Windows</ComboBoxItem>
                        </ComboBox>
                    </StackPanel>

                    <Button Content="Apply Keybinds" Click="ApplyKeybinds_Click" Margin="0,15,0,0" Width="120" HorizontalAlignment="Left"/>
                </StackPanel>
            </GroupBox>

            <!-- Bot Behavior Settings -->
            <GroupBox Header="Bot Behavior" Margin="0,0,0,15">
                <StackPanel Margin="10">
                    <CheckBox x:Name="AutoHeal" Content="Auto-heal nearby allies" IsChecked="{Binding AutoHeal, Mode=TwoWay}"/>
                    <CheckBox x:Name="AutoUber" Content="Auto-uber at critical health" IsChecked="{Binding AutoUber, Mode=TwoWay}"/>
                    <Label Content="Uber Charge Threshold (%):"/>
                    <Slider x:Name="UberThreshold" Minimum="0" Maximum="100" Value="{Binding UberThreshold, Mode=TwoWay}"/>
                    <TextBlock Text="{Binding ElementName=UberThreshold, Path=Value, StringFormat={}{0:F0}%}"/>
                </StackPanel>
            </GroupBox>

            <Button Content="Save All Settings" Click="SaveAll_Click" Width="150" Height="30" Margin="0,20"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>