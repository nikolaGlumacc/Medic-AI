<UserControl x:Class="MedicAIGUI.Views.MatrixView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1000">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Padding="30,30,30,0">
            <StackPanel>
                <TextBlock Text="TRIAGE PRIORITY MATRIX" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,0,5,0"/>
                <TextBlock Text="Real-time unit analysis and heuristic heal-priority weights." Style="{StaticResource MicroLabel}"/>
            </StackPanel>
        </Border>

        <!-- MAIN MATRIX CONTENT -->
        <Grid Grid.Row="1" Margin="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="320"/>
            </Grid.ColumnDefinitions>

            <!-- UNIT LIST (SIMULATED FOR PREMIUM LOOK) -->
            <Border Background="{StaticResource BgCardBrush}" CornerRadius="28" Padding="25" BorderBrush="#15FFFFFF" BorderThickness="1" Margin="0,0,20,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <UniformGrid Columns="4" Margin="10,0,10,15">
                        <TextBlock Text="UNIT IDENTIFIER" Style="{StaticResource MicroLabel}"/>
                        <TextBlock Text="CLASS" Style="{StaticResource MicroLabel}"/>
                        <TextBlock Text="VITAL STABILITY" Style="{StaticResource MicroLabel}"/>
                        <TextBlock Text="HEAL WEIGHT" HorizontalAlignment="Right" Style="{StaticResource MicroLabel}"/>
                    </UniformGrid>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="UnitList">
                            <!-- Example Row 1 -->
                            <Border Background="#0C121B" CornerRadius="12" Padding="15" Margin="0,0,0,10">
                                <UniformGrid Columns="4">
                                    <TextBlock Text="UNIT_HEAVY_01" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                                    <TextBlock Text="HEAVY" Opacity="0.6" VerticalAlignment="Center"/>
                                    <Grid VerticalAlignment="Center" Margin="0,0,20,0">
                                        <Border Height="6" Background="#11FFFFFF" CornerRadius="3"/>
                                        <Border Height="6" HorizontalAlignment="Left" Width="60" Background="{StaticResource AccentHotBrush}" CornerRadius="3"/>
                                    </Grid>
                                    <TextBlock Text="92.4%" HorizontalAlignment="Right" FontWeight="Bold" Foreground="{StaticResource AccentHotBrush}" VerticalAlignment="Center"/>
                                </UniformGrid>
                            </Border>
                            <!-- Example Row 2 -->
                            <Border Background="#0C121B" CornerRadius="12" Padding="15" Margin="0,0,0,10">
                                <UniformGrid Columns="4">
                                    <TextBlock Text="UNIT_SOLDIER_09" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                                    <TextBlock Text="SOLDIER" Opacity="0.6" VerticalAlignment="Center"/>
                                    <Grid VerticalAlignment="Center" Margin="0,0,20,0">
                                        <Border Height="6" Background="#11FFFFFF" CornerRadius="3"/>
                                        <Border Height="6" HorizontalAlignment="Left" Width="180" Background="{StaticResource AccentGreenBrush}" CornerRadius="3"/>
                                    </Grid>
                                    <TextBlock Text="45.2%" HorizontalAlignment="Right" FontWeight="Bold" Foreground="{StaticResource AccentGreenBrush}" VerticalAlignment="Center"/>
                                </UniformGrid>
                            </Border>
                            <!-- More rows would be dynamic -->
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- HEURISTICS PANEL -->
            <StackPanel Grid.Column="1">
                <Border Background="{StaticResource BgCardBrush}" CornerRadius="28" Padding="25" BorderBrush="#15FFFFFF" BorderThickness="1" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="◈  HEURISTIC WEIGHTS" Style="{StaticResource SectionTitle}" Margin="0,0,0,20"/>
                        
                        <TextBlock Text="POWER CLASS MULTIPLIER" Style="{StaticResource MicroLabel}"/>
                        <DockPanel Margin="0,0,0,15">
                            <TextBlock Text="2.5x" DockPanel.Dock="Right" FontWeight="Bold" Foreground="{StaticResource AccentCoolBrush}"/>
                            <Slider Minimum="1.0" Maximum="5.0" Value="2.5"/>
                        </DockPanel>

                        <TextBlock Text="SUPPORT CLASS MULTIPLIER" Style="{StaticResource MicroLabel}"/>
                        <DockPanel Margin="0,0,0,15">
                            <TextBlock Text="1.2x" DockPanel.Dock="Right" FontWeight="Bold" Foreground="{StaticResource AccentCoolBrush}"/>
                            <Slider Minimum="1.0" Maximum="5.0" Value="1.2"/>
                        </DockPanel>

                        <TextBlock Text="DISTANCE PENALTY" Style="{StaticResource MicroLabel}"/>
                        <Slider Minimum="0.0" Maximum="1.0" Value="0.4" Margin="0,0,0,15"/>
                    </StackPanel>
                </Border>

                <Button Content="APPLY HEURISTICS" Style="{StaticResource PrimaryBtn}" Height="50" HorizontalAlignment="Stretch"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
